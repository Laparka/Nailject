{%- for namespace in namespaces -%}
    {%- if namespace == "" -%}
export const not_defined_symbol_namespace = {
    {%- else -%}
export const {{ namespace }} = {
    {%- endif -%}
    {%- for symbol in symbols -%}
        {% if symbol.symbolNamespace == namespace %}
    {{ symbol.symbolId }}: Symbol.for('{{ symbol.symbolId }}'),
        {%- endif -%}
    {% endfor %}
};

{% endfor %}